import React, { memo } from 'react'
import { LoadingWrapper, LoadingContent } from './style'
const Loading = memo(() => {
  return (
    <LoadingWrapper>Loading</LoadingWrapper>
  )
})

export default Loading