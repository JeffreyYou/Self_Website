import React, { memo } from 'react'
import { motion } from 'framer-motion'

const Project = memo(() => {
  return (
    <motion.div
    
    >
      Project
    </motion.div>
  )
})

export default Project